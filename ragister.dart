import 'package:flutter/material.dart';

class ragister extends StatelessWidget {
  const ragister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ragister"),
      )
    );
  }
}